import React from 'react';
import { Typography } from '@mui/material';

const WorkersPage: React.FC = () => (
  <div>
    <Typography variant="h4" gutterBottom>Workers</Typography>
    <Typography>Manage and monitor worker nodes here.</Typography>
  </div>
);

export default WorkersPage;